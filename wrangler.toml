name = "tt"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
# 在 CF Dashboard 的 Workers Settings 里配置，或用 wrangler secret put
# OPENAI_API_KEY = "sk-xxx"
# OPENAI_BASE_URL = "https://api.openai.com/v1"  # 可换成任意兼容接口
# ADMIN_TOKEN = "your-admin-token"

[[kv_namespaces]]
binding = "KB"
id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# 部署前先创建: wrangler kv namespace create KB
# 然后把返回的 id 填到上面
